@using Microsoft.AspNetCore.SignalR.Client
@using Microsoft.JSInterop
@inject NavigationManager Navigation
@inject IJSRuntime JS

<h3>Component</h3>
<button @onclick="Send">Send</button>

@code {
    private HubConnection? hubConnection;

    protected override async Task OnInitializedAsync()
    {
        hubConnection = new HubConnectionBuilder()
            .WithUrl(Navigation.ToAbsoluteUri("/authhub"))
            .Build();

        hubConnection.On<string>("ReceiveMessage", async (id) =>
        {
            var encodedMsg = $"{id}";
            await JS.InvokeVoidAsync("console.log", id);
        });

        await hubConnection.StartAsync();
    }

    private async Task Send()
    {
        await JS.InvokeVoidAsync("console.log", "test");
        if (hubConnection is not null)
        {
            await hubConnection.SendAsync("SendMessage", "test");
        }
    }

    public bool IsConnected =>
        hubConnection?.State == HubConnectionState.Connected;
}
